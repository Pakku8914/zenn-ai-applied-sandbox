"""あなたの答えを書くファイル（復習03の問題1・問題9）。

**練習問題を解く前に、2つの答えの中身を消してから始めてください。**
（`Q1 = {}` / `Q9 = []` のように空にする。元に戻すときは `git checkout src/review03/answers.py`）

答え合わせは機械が行う。

    docker compose exec app python src/review03/verify.py
"""

from __future__ import annotations

# --- 問題1：13項目のレビュー ------------------------------------------------
# `python src/review03/spec.py` の出力だけを見て、13項目それぞれについて
#   「穴か」   … True（指摘する） / False（指摘しない）
#   「塞ぐ層」 … 穴なら層の名前、穴でなければ "—"
# を書く。層の名前は findings.PRIORITY のものだけを使う（表記を揺らすと機械が判定できない）。
#   権限制限 / 隔離の境界 / 出力検査 / 記録との照合 / 再試行の判断 /
#   人間承認 / 入力検査 / 構造分離 / プロンプト
Q1 = {
    "F1": {"穴か": True, "塞ぐ層": "隔離の境界"},
    "F2": {"穴か": True, "塞ぐ層": "隔離の境界"},
    "F3": {"穴か": True, "塞ぐ層": "権限制限"},
    "F4": {"穴か": True, "塞ぐ層": "権限制限"},
    "F5": {"穴か": True, "塞ぐ層": "人間承認"},
    "F6": {"穴か": True, "塞ぐ層": "人間承認"},
    "F7": {"穴か": True, "塞ぐ層": "再試行の判断"},
    "F8": {"穴か": True, "塞ぐ層": "記録との照合"},
    "F9": {"穴か": True, "塞ぐ層": "出力検査"},
    "F10": {"穴か": True, "塞ぐ層": "記録との照合"},
    "N1": {"穴か": False, "塞ぐ層": "—"},
    "N2": {"穴か": False, "塞ぐ層": "—"},
    "N3": {"穴か": False, "塞ぐ層": "—"},
}

# --- 問題9：残余リスク ------------------------------------------------------
# findings.RESIDUAL の5件のうち、**本当に残るもの**の ID だけを並べる（昇順）。
# 測れる層で塞げるものを残余リスクに混ぜると、報告書が「諦めの一覧」になる。
Q9 = ["R1", "R2", "R3"]
