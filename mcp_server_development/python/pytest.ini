[pytest]
# src/ をインポートパスに含める（src/test_server.py が src/server.py を import できるように）
pythonpath = src
testpaths = src
# 非同期テストは @pytest.mark.asyncio を明示する方針（strict モード）。
# asyncio_mode = auto にするとマーカー不要になるが、どのテストが非同期か読み取りにくくなる
