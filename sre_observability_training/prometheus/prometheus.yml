global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Collector が変換して公開するアプリのメトリクス
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]

  # Prometheus 自身
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
