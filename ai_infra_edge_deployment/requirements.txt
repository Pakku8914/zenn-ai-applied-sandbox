# 再現性のためバージョンを固定する（2026-08-15 時点で解決した組み合わせ）
httpx==0.28.1
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
onnx==1.22.0
onnxruntime==1.28.0
numpy==2.5.2
pytest==9.1.1
huggingface-hub==1.27.0
# 推移的に入るが、S08 の k8s マニフェスト検証が直接依存するので明示して固定する
PyYAML==6.0.3
