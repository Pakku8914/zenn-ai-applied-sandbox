# 再現性のためバージョンを固定する（2026-08-15 時点で解決した組み合わせ）
sentence-transformers==5.7.0
transformers==4.57.6
# torch は **CPU 専用 wheel** をバージョン文字列（+cpu）まで含めて固定する。
# 既定の PyPI から `torch==2.13.0` を入れると、CPU で使う場合でも CUDA 関連の
# パッケージ（nvidia-*）を数GB引っ張ってくる（本書のイメージの実測: 8.85GB 対 2.12GB）。
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
numpy==2.5.2
qdrant-client==1.19.0
janome==0.5.0
pytest==9.1.1
anthropic==0.120.2
python-dotenv==1.2.2
