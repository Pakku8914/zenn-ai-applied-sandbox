# 検索設計書：みなと商事 社内文書検索基盤

> 最終プロジェクトの成果物①のテンプレートです。空欄を埋めて使ってください。
> **「決めたこと」より「なぜそう決めたか」と「何を棄却したか」に紙面を使います。**
> 見出しは自己検証スクリプトが存在を確認しています。消さずに中身を埋めてください。

- 作成日：YYYY-MM-DD ／ 作成者：
- 対象コーパス：`corpus/docs.jsonl`（301文書）／判定データ：`corpus/qrels.jsonl`（622件）
- 測定環境：（アーキテクチャ / CPUコア数 / メモリ / Python のバージョン）
- 再利用した資産：`python src/final/reuse.py` の出力を貼る

## 1. この検索基盤が満たすべきこと

- 利用者と使われ方：
- 主要指標（最大化するもの）と目標：
- **絶対に守る制約**（下回ったら他の数字を見ずに止めるもの）：
- 明示的に扱わないこと：

## 2. 決定事項

| # | 決めたこと | 採用した値 | 理由 | 根拠となる測定 | 棄却した選択肢と棄却の理由 |
| :-- | :--- | :--- | :--- | :--- | :--- |
| D1 | チャンク方式 | | | | |
| D2 | 1段目の検索方式 | | | | |
| D3 | 略語への打ち手 | | | | |
| D4 | 2段目（リランク）の有無と候補数 | | | | |
| D5 | 権限フィルタの掛け方 | | | | |
| D6 | 生成に渡す件数とコンテキスト上限 | | | | |
| D7 | 後処理（引用検証・回答不能）の判定順序 | | | | |

## 3. 凍結した条件（統制変数）

一度に複数を動かすと、どれが効いたか言えなくなります。**合成カセットの鍵に効く条件**は
とくに明記してください（変えたら作り直しが要る＝改善のコストに含める）。

| 固定したもの | 値 | 固定した理由 | 動かすと何が壊れるか | カセットの作り直し |
| :--- | :--- | :--- | :--- | :--- |
| | | | | 要 / 不要 |

- 構成を変えたとき、古いカセットに当たらなくなったクエリ数：____ / 120 件

## 4. 権限モデル

| 役割 | 見てよい `visibility` | 払い出し元 | 未知の役割が来たときの挙動 |
| :--- | :--- | :--- | :--- |
| | | | |

- フィルタを掛ける位置（事前 / 事後）と、その理由：
- 空の条件（`{}`）を返さないための実装上の約束：
- キャッシュキーに含める権限スコープ：
- 混入検査の対象（クエリ集合 × 役割）と結果：____ 件中 混入 ____ 件

## 5. 既知の弱点

| 弱点 | 影響するクエリ型 | 実測値 | 現時点の扱い | 再検討する条件 |
| :--- | :--- | --: | :--- | :--- |
| | | | | |

## 6. 再現手順

この設計書の数字を再現するコマンドを、コピー&ペーストで動く形で書きます。

```bash
docker compose up -d
docker compose exec app python src/final/final_cassette.py
docker compose exec app python src/final/evaluate.py
docker compose exec app python src/final/verify.py
```

- 生成される成果物：
- 所要時間の目安：
- 再現できない条件（環境依存で変わる値）：
